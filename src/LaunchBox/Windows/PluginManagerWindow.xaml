<Window x:Class="LaunchBox.Windows.PluginManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Plugin Manager - LaunchBox"
        Height="500" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Installed Plugins" FontSize="24" FontWeight="Bold"
                   Foreground="White" Margin="0,0,0,20"/>

        <DataGrid Grid.Row="1" x:Name="PluginsDataGrid" AutoGenerateColumns="False"
                 Background="#252526" Foreground="White" BorderBrush="#3E3E42"
                 RowBackground="#2D2D30" AlternatingRowBackground="#252526"
                 GridLinesVisibility="None" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="80"/>
                <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,20,0,0">
            <TextBlock x:Name="PluginCountText" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button Content="Open Plugins Folder" Click="OpenPluginsFolder_Click" Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Reload Plugins" Click="ReloadPlugins_Click" Padding="15,8" Margin="0,0,10,0"/>
            <Button Content="Close" Click="Close_Click" Padding="15,8"/>
        </StackPanel>
    </Grid>
</Window>
